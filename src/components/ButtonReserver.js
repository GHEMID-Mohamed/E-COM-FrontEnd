import React, { Component } from 'react';
import { Button } from 'reactstrap'

export default class ReserverButton extends Component {
    render() {
        return (
            <div>
                <Button color="primary">primary</Button>{' '}
            </div>
        );
    }
}

